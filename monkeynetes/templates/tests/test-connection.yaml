apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "monkeynetes.fullname" . }}-test-connection"
  labels:
{{ include "monkeynetes.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "monkeynetes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
